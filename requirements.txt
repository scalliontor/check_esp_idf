# Python dependencies for test_wake_net

# --- Web server ---
fastapi>=0.110
uvicorn[standard]>=0.24

# --- Core / numerics ---
numpy>=1.24
soundfile>=0.12

# --- VAD (Silero via torch.hub) ---
torch>=2.1
torchaudio>=2.1
torchcodec>=0.2

# --- STT (Zipformer with sherpa-onnx) ---
sherpa-onnx>=1.10
onnxruntime>=1.17

# --- LLM (Gemini) ---
google-genai>=0.3

# --- TTS (ZipVoice) ---
# Pull all runtime deps for ZipVoice from its own file
-r ZipVoice/requirements.txt

# --- Optional utilities ---
tqdm>=4.65
librosa>=0.10  # advanced audio ops (optional)
resampy>=0.4.2 # higher-quality resampling (optional)